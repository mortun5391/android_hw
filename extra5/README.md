# extra5
- Новости берутся с NewsAPI и кешируются в Room.
- При ошибке сети показывается кеш и сообщение.
- Изображения загружаются через Glide.
- Для управления памятью: Glide использует собственный memory/disk cache, загрузка идёт в `RecyclerView`.
